#include <stdio.h>

/**
 * main - entry point
 * description - C program that print all single digit numbers
 * of base 10starting from 0
 * Return: it always 0
 */

int main(void)
{
int i;
for (i = 0; i < 10; i++)
{
printf("%d\n", i);
}
return (0);
}
